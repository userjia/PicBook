<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.picbook">

    <application
        android:name="com.example.common.DataTemp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BookListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".BookEditActivity">
        </activity>
        <activity
            android:name=".PageListActivity">
        </activity>
        <activity
            android:name=".PageActivity">
        </activity>
        <activity
            android:name=".PageEditActivity">
        </activity>

        <activity
            android:name=".PicDrawActivity">
        </activity>

    </application>

</manifest>